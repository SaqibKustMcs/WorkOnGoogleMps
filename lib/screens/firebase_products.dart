import 'package:flutter/material.dart';

class FirebaseProducts extends StatelessWidget {
  const FirebaseProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Firebase Products"
        ),

      ),
      body:
      Column(
        children: [

        ],
      ),
    );
  }
}
